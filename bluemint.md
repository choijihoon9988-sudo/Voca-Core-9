# Voca-Core 9 (v3.3) - 수능 스페인어 3등급 목표 앱

## 1. 프로젝트 목표
9개년 수능 스페인어 기출 PDF를 분석하여 추출한 핵심 빈출 단어를 효율적으로 학습하는 웹 앱을 개발한다. 사용자의 목표는 수능 5등급(필수) 및 3등급(베스트) 달성이다.

## 2. 핵심 기능 (v3.3 기준)
1.  **스테이지 기반 학습 (v3.2):**
    * `Stage 1 (3등급 Core)`: 빈출 1~45위 (45단어)
    * `Stage 2 (2등급 Core)`: 빈출 46~120위 (75단어)
    * `Stage 1` 100% 달성 시 `Stage 2`가 자동으로 잠금 해제된다.
2.  **4가지 학습 모드 (`index.html`):**
    * 플래시카드: 단어 암기 (기본)
    * '몰라요' 복습: '몰라요'로 체크한 단어만 복습
    * 문법 퀴즈: 예문 속 올바른 단어 형태 고르기
    * 예문 퀴즈: 예문 빈칸에 알맞은 표제어 고르기
3.  **학습 진행률 저장:**
    * `localStorage`를 사용하여 스테이지별/단어별 학습 상태('알아요'/'몰라요') 및 잠금 해제 상태를 저장한다.
4.  **전체 단어 목록 (`wordlist.html`):**
    * 전체 단어(v3.2 기준 120개)를 빈출 순서(Stage 순서)대로 나열한다.
    * **(v3.3)** '주제별 필터' 탭을 제공하여 원하는 주제의 단어만 모아볼 수 있다.
    * 단어 클릭 시 상세 정보(예문, 기출 형태 등)가 담긴 모달이 표시된다.
5.  **PDF 자료실 (`pdf_library.html`):**
    * 분석에 사용된 원본 기출 PDF 9종을 열람할 수 있다.

## 3. 프로젝트 파일 구조 (public)
-   `index.html`: 메인 학습 페이지 (v3.3: '주제 필터' UI 및 관련 로직 제거. 학습 흐름 단순화.)
-   `wordlist.html`: 전체 단어 목록 페이지 (v3.3: '주제 필터' 탭 UI 및 필터링 로직 추가. '주제' 열 추가.)
-   `pdf_library.html`: PDF 기출 자료실 페이지
-   `스페인어.txt`: 핵심 단어 데이터 (v3.2: Stage 1, 2 통합 120단어 JSON)
-   `01.pdf` ~ `09.pdf`: 원본 기출 PDF 파일

## 4. 변경 기록 (History)
-   **v3.0 (2025-10-27):** 초기 MVP. 45단어(Stage 1) 및 4가지 학습 모드 구현.
-   **v3.1 (2025-10-28):** '스테이지 시스템' 도입. (`index.html`, `스페인어.txt` 수정)
    -   `스페인어.txt`에 "stage": 1 속성 추가.
    -   `index.html`에 스테이지 탭 UI 및 잠금 해제 로직 추가.
-   **v3.2 (2025-10-28):** 'Stage 2' 데이터 75개 통합. (총 120단어)
    -   `스페인어.txt`에 "stage": 2 데이터 75개 병합.
    -   `wordlist.html`에 Stage 정보 표시 기능 추가.
-   **v3.3 (2025-10-28):** UX/UI 최적화. (역할 분리)
    -   `index.html`에서 '주제 필터' 기능 제거하여 학습 흐름 단순화.
    -   `wordlist.html`로 '주제 필터' 기능을 이관 (필터 탭 및 '주제' 열 추가).